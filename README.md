# CODIGOS

This repository contains a simple React application scaffolded manually for demonstration purposes. The application is bootstrapped with Vite and includes two basic components:

- **ArticleList**: Displays a list of article titles.
- **ArticleForm**: Provides a form to add new articles.

Run `npm run dev` to start the development server after installing dependencies.
